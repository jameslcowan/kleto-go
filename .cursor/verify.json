{
  "commands": [
    {
      "name": "agent-attribution",
      "run": "bash -c 'source .cursor/hooks/lib/agent-attribution.sh && c=$(agent_attribution_count) && test \"${c:-0}\" -eq 0'",
      "timeout": 30
    }
  ]
}
